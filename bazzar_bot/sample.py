# import pygetwindow

# #titles = pygetwindow.getAllTitles()
# #print(titles)
# activeWindow = pygetwindow.getWindowsWithTitle('Wizard101')[0]

# left, top = activeWindow.topleft
# right, bottom = activeWindow.bottomright

# print(left, top, right, bottom)
int templ_depth = nextpage.png.depth();
int img_depth = _img.depth();

if (templ_depth != img_depth) {
    // Handle the mismatch (e.g., convert one of the images to the other's depth)
}

int templ_type = _templ.type();
int img_type = _img.type();

if (templ_type != img_type) {
    // Handle the mismatch (e.g., convert one of the images to the other's type)
}
